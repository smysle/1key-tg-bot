services:
  bot:
    image: ghcr.io/smysle/1key-tg-bot:latest
    container_name: 1key-tg-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
    networks:
      - grok2api_default

networks:
  grok2api_default:
    external: true
