services:
  bot:
    build: .
    container_name: 1key-tg-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
    # 如果需要 Redis
    # depends_on:
    #   - redis
    networks:
      - 1key-net

  # 可选：Redis 用于持久化
  # redis:
  #   image: redis:7-alpine
  #   container_name: 1key-redis
  #   restart: unless-stopped
  #   volumes:
  #     - redis-data:/data
  #   networks:
  #     - 1key-net

networks:
  1key-net:
    driver: bridge

# volumes:
#   redis-data:
